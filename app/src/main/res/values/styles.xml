<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="loginEditTextBackground">@color/loginEditTextBackground</item>
        <item name="loginIconBackground">@color/loginIconBackground</item>
    </style>
    
    <style name="DarkTheme" parent="Base.AppTheme.Dark"/>

    <style name="Base.AppTheme.Dark" parent="Theme.AppCompat">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="loginEditTextBackground">@color/loginEditTextBackground</item>
        <item name="loginIconBackground">@color/loginIconBackground</item>
    </style>

</resources>
